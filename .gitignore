# Données NSD / COCO — JAMAIS commit (contrainte de licence, cf. spec §4.5)
/data/
# Sorties de build (images, artefacts) — un petit échantillon pourra être commit explicitement
/artifact/
/artifact_*/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.env
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Node / Vite
node_modules/
dist/
.vite/
*.local

# Playwright
test-results/
playwright-report/
blob-report/

# Modèles / poids volumineux
*.ckpt
*.safetensors
*.pth
*.hdf5

# OS / éditeur
.DS_Store
.idea/
.vscode/

# Local full artifact build (~1 GB, 982 items incl. all COCO ground-truth images).
# Only the small demo fixture (already tracked) is shipped; the rest is generated
# locally by the lab pipeline and must not be committed (size + NSD/COCO licensing).
/app/public/sample-artifact/
